import { ChangeVocalsToNumbersPipe } from './change-vocals-to-numbers.pipe';

describe('ChangeVocalsToNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVocalsToNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
